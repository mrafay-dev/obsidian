Virtual Dynamic Shared Object

kernel mechanism to allow processes to access kernel space routines without incurring a 'mode switch' penalty 

That penalty happens when we switch from user space to kernel space - system call interface
