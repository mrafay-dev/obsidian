
Doubly linked list with non-contiguos memory
Has O1 insert and delete without invalidating iterators
Has more cache misses and pointer chasing, so slower iteration

std::vector has better cache locality and faster random access

	